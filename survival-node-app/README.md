# Survival Node User Guide — Android App Source

This folder holds the source for the **Survival Node User Guide** Android app.

- [`index.html`](index.html) — the complete, self-contained app (inline CSS/JS, no external
  dependencies). This single file **is** the app. Edit it here, then rebuild the Android app.

## How the app is built

The app is **not** compiled with Android Studio/Gradle. It is generated by
[WebIntoApp.com](https://www.webintoapp.com/), which wraps `index.html` in a WebView Android app
and produces the signed APK/AAB.

| Field | Value |
|---|---|
| App name | Survival Node User Guide |
| Package | `com.easternshoreaillc.survivalnodeuserguide` |
| WebIntoApp App ID | 1240101 |
| Current source version | 1.2 (set versionCode 102 at next build) |
| Last built version | 1.1 (versionCode 101) |
| Edit/rebuild URL | https://webintoapp.com/author/apps |

**Signing key & build artifacts** (keep private — not in this repo):
`~/Desktop/STUFF/Important_Documents/ESAI/Survival Node Software/User Guide Files/`
- `Survival Node User Guide 1.1/android/my-release-key.jks` — **reuse this same keystore** for
  every update, or the Play Store will reject the upload as a different app.
- `readme.txt` / `android/certification.txt` — keystore password, app password, App Key.

## To update the app

1. Edit `index.html` in this folder.
2. Keep it in sync with the online guide at `/node-setup.html` and the warranty page at
   `/warranty-insert.html` (the canonical return/warranty policy).
3. Re-upload `index.html` to WebIntoApp app #1240101, bump the version (e.g. 1.2 / versionCode 102),
   and rebuild using the **same keystore**.

## Version history

- **1.1** (versionCode 101) — original build (Gen 1).
- **1.2** (versionCode 102 — source ready, not yet built) — Gen 2 content update: Mission Darkness
  Faraday bags, 24,000 mAh solar battery (3 attached cables + side USB-A), 2 offline LLMs with
  Qwen 3.5-4B as primary (Qwen 2.5-3B fast backup), and warranty section aligned to the site (buyer
  pays return shipping, return eligibility, support phone number). Footer stamped `v1.2`.
